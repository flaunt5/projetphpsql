<div class="row">
    <div class="col-sm-12">
        <table width="100%"
               class="table table-striped table-bordered table-hover dataTable no-footer dtr-inline"
               id="dataTables-example" role="grid"
               aria-describedby="dataTables-example_info" style="width: 100%;">
            <thead>
            <tr role="row">
                <th class="sorting_asc" tabindex="0" aria-controls="table_test"
                    rowspan="1" colspan="1" aria-sort="ascending"
                    aria-label="Editer"
                    style="width: 161px;">Editer
                </th>
                {% for name in column %}
                    <th class="sorting_asc" tabindex="0" aria-controls="table_test"
                        rowspan="1" colspan="1" aria-sort="ascending"
                        aria-label="{{ name }}"
                        style="width: 161px;">{{ name }}
                    </th>
                {% endfor %}
            </tr>
            </thead>
            <tbody>
            {% for key2,line in table %}
                <tr class="gradeA even" role="row">

                    <td><button type="button" class="btn btn-outline btn-info viewAction editrow">Editer</button>

                        <div class="col-lg-12"> </div>
                        <button type="button" class="btn btn-outline btn-danger">Supprimer</button></td>
                    {% for key,value in line %}
                        <td class="rowselect">{{ value }}</td>
                    {% endfor %}
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <button type="button" class="btn btn-primary" data-toggle="modal" data-target=".bs-example-modal-lg">Large modal</button>
        <div class="modal fade bs-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel">
            <div class="modal-dialog modal-lg" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-label="Fermer"><span aria-hidden="true">&times;</span></button>
                        <h4 class="modal-title" id="myModalLabel">Edition d'un tuple</h4>
                    </div>
                    <div class="modal-body">
                        <form action="" role="form">
                        {% for name in column %}
                            <div class="form-group">
                                <label>Edition du champ : {{ name }}</label>
                                <input type="text" class="form-control formjs" name="{{ name }}">
                            </div>
                        {% endfor %}
                            <input type="submit" type="button" class="btn btn-primary" value="Sauvegarder">
                        </form>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-default" data-dismiss="modal">Fermer</button>
                    </div>
                </div>
            </div>
        </div>


    </div>
</div>
{{ nbInput }}
<script>
    $(".editrow").on('click', function (e) {
        console.debug("test");
        var elem = $(".rowselect").first();
        elem.addClass("text-danger");
        elem = elem.next();
        elem.addClass("text-danger");
        elem = elem.next();
        elem.addClass("text-danger");
        elem = elem.next();
        elem.addClass("text-danger");
    });
</script>
